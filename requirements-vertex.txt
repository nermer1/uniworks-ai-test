# vertex_ai provider(litellm) 쓰는 배포에서만 설치 — litellm이 GCP 인증에 사용.
#   uv pip install -r requirements-vertex.txt
# Gemini(AI Studio)·OpenAI·Claude 등만 쓰면 불필요 (litellm이 표준 키로 처리).
google-auth
requests      # google.auth.transport.requests가 내부적으로 사용
