# ─────────────────────────────────────────────────────────
# 코어 — 모든 배포에 항상 설치 (모듈/provider 무관)
# ─────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
python-multipart==0.0.20

# 선택 의존성은 산 것만 설치 (à la carte):
#   OCR 모듈 쓰면:          uv pip install -r requirements-ocr.txt
#   vertex_ai provider 쓰면: uv pip install -r requirements-vertex.txt
#   recommend 모듈 쓰면:     uv pip install -r requirements-recommend.txt
